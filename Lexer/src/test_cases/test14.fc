int :: a, b

cin >> a >> b

cin >> a
cin >> b

if(a > b){
    cin >> a >> b
    cin >> a
    cin >> b
}
a = 100
else{
    cin >> a >> b
    cin >> a
    cin >> b
}

for(int i = 0; i<a; i++){
    cin >> a >> b
    cin >> a
    cin >> b
    if(a > 0)
        cin >> a
}